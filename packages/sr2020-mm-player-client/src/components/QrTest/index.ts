// import { withTranslation } from 'react-i18next';
import { pipe } from 'ramda';
import { QrTest } from './QrTest';

// const tmp = pipe(withTranslation())(QrTest);
const tmp = (QrTest);

export { tmp as QrTest };