// import { withTranslation } from 'react-i18next';
import { pipe } from 'ramda';
import { SpiritPage } from './SpiritPage';

// const tmp = pipe(withTranslation())(SpiritPage);
const tmp = (SpiritPage);

export { tmp as SpiritPage };