export class Bot {
  constructor(speed, path, index, waitTime, fraction) {
    this.speed = speed;
    this.path = path;
    this.index = index;
    this.waitTime = waitTime;
    this.fraction = fraction;
  }
}
